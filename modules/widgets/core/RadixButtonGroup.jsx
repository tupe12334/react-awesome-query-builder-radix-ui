import React from "react";
import * as ToggleGroup from "@radix-ui/react-toggle-group";

export default function RadixButtonGroup({ children, config }) {
  return <div className="qb-radix-button-group">{children}</div>;
}
